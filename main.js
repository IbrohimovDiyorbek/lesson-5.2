let ism = prompt("Ismingizni kiriting!");
let id = prompt("Space ID yingizni kiriting!");
let parol = prompt("Space password ingizni kiriting!");

const diyorbek = {
    name: ism,
    id: id,
    password: parol,
};
console.log(diyorbek);